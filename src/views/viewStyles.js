import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
	clock_wall: {
		display: 'flex',
	},
	container: {
		position: 'absolute',
		height: '100%',
		width: '100%',
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
	},
}));
